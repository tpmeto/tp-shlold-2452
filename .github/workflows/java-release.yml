name: java:release
on:
  workflow_dispatch:
  push:
    branches:
      - main

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  GITHUB_USER: "tp-robot"

permissions:
  contents: write
  actions: read

concurrency:
  group: java-release
  cancel-in-progress: false

jobs:
  build:
    name: java-release
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref || github.ref_name }}

      - name: Check for Java changes
        id: java_changes
        shell: bash
        run: |
          git fetch origin main --depth=2
          CHANGED=$(git diff --name-only HEAD^ HEAD -- 'java/applications/**' | grep . || true)
          if [ -z "$CHANGED" ]; then
            echo "No Java files changed. Skipping workflow."
            exit 0
          fi

      - name: Release Java applications
        id: java_release
        shell: bash
        run: |
          echo "Releasing Java applications..."
          
          # sleep for 1m
          echo "Sleeping for 1 minutes to simulate work."
          sleep 60
          
          # increment the version number in the version.properties file
          VERSION_FILE="$(git rev-parse --show-toplevel)/java/applications/version.properties"
          if [ -f "$VERSION_FILE" ]; then
            CURRENT_VERSION=$(cat $VERSION_FILE)
            echo "Current version: $CURRENT_VERSION"
            IFS='.' read -ra VERSION_PARTS <<< "$CURRENT_VERSION"
            MAJOR=${VERSION_PARTS[0]}
            MINOR=${VERSION_PARTS[1]}
            PATCH=${VERSION_PARTS[2]}
            PATCH=$((PATCH + 1))
            NEW_VERSION="$MAJOR.$MINOR.$PATCH"
            echo "New version: $NEW_VERSION"
            echo "$NEW_VERSION" > $VERSION_FILE
          else
            echo "0.1.0" > $VERSION_FILE
            echo "Version file not found. Starting with version 0.1.0"
          fi
          
          # commit the version change
          git config --local user.name $GITHUB_USER
          git add $VERSION_FILE
          git commit -m "Release Java applications - version $NEW_VERSION"
          git push origin HEAD:main
          
          echo "Java Release complete..."
          
